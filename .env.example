# Upstash Redis (auto-injected by the Vercel Marketplace integration).
# Either naming pair works; leave both unset to use the in-memory dev store.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Absolute origin, used for Open Graph / metadata. Optional.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
